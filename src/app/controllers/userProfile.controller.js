// Generated by CoffeeScript 1.10.0
(function() {
  angular.module('quizical')
  .controller('UserProfileController', UserProfileController);

  function UserProfileController($stateParams, $state, toastr, UserProfileService) {
    var vm = this;
    vm.user = {};
    vm.editUserProfileForm = true;

    vm.showUserEditForm = function () {
      if (vm.editUserProfileForm === true) {
        vm.editUserProfileForm = false;
      } else {
        vm.editUserProfileForm = true;
      }
    };

    UserProfileService.getUserProfile('1')
      .then(
        function (success) {
          vm.user = success.data;
        },
        function (error) {
          toastr.error('Failed to show your profile', 'ERROR!');
        }
      );
  }
}).call(this);
